# App
node_env=development
port=3000
api_prefix=/api/v1
cors_origin=http://localhost:5173

# MongoDB
mongodb_uri=mongodb://localhost:27017/case_management
mongodb_db_name=case_management

# Auth / passwords
jwt_secret=change-me
jwt_expires_in=15m
jwt_refresh_secret=change-me-too
jwt_refresh_expires_in=7d
bcrypt_rounds=12

# Encryption at rest for OAuth tokens (32-byte key, base64)
token_encryption_key=

# Google OAuth (tenant mailbox)
google_client_id=
google_client_secret=
google_oauth_redirect_base=http://localhost:3000/api/v1/tenants

# Workers / Gmail
gmail_pubsub_topic=
email_poll_interval_ms=60000

# URLs for onboarding and password-reset emails
public_app_url=http://localhost:5173
email_from_name=Case Management

# Internal HTTP triggers (optional)
internal_api_key=
email_worker_enabled=true

# Logging
log_level=info
